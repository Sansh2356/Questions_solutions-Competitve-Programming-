#include <iostream>
#include <stack>
#include <queue>
using namespace std;
// Q)Circular Tour//
int main()
{
    for (int x = 0; x < n.size(); x++)
    {
        int balance = 0;
        int front = 0;
        int rear = 0;
        balance = balance + petrol - distanc;
        if (balance > 0)
        {
            rear++;
        }
        else
        {

            front = rear + 1;
            rear = front;
        }
        
    }
    if(rear==front){
        return index;
        //cycle is complete//
    }
   
}